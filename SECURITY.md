# Security Policy

## 信任模型

MyClaw 是一个**单用户本地 AI 助手**，设计上不假设多租户隔离。

- 运行 Gateway 的用户被视为该实例的唯一可信操作者
- WebSocket 和 HTTP 端点默认绑定到本地回环（`localhost:18789`）
- 工具执行（read_file / write_file / execute_shell）等同于用户本人执行

## 已知风险

MyClaw 目前**没有沙箱**：

- `execute_shell` 工具直接在主机上运行命令
- `write_file` 可覆盖任意可写路径
- AI 模型可能发出意外或危险的工具调用请求

**建议**：

- 仅在个人设备上使用，不要暴露到公网
- 不要在与他人共享的操作系统账户上运行
- 需要隔离时，在虚拟机或容器中运行 Gateway

## 报告安全问题

如果你发现安全漏洞，请直接联系仓库维护者。报告时请包含：

1. 受影响组件和版本
2. 复现步骤
3. 实际影响范围
4. 修复建议（如有）

## 不属于安全漏洞的情况

- AI 模型发出有害的 Shell 命令请求（这是模型行为，而非系统漏洞）
- 本地文件被工具读取或写入（这是预期功能）
- 未配置认证时 Gateway 被本地其他进程访问（默认绑定 localhost）
